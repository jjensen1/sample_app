<!DOCTYPE html>
<html>
<head>
  <title>SFERex CONsult</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1"> 
    <!-- "viewport" formats the header for mobile devices -->
</head>
<body>
	
	<%= render 'layouts/header' %> 
	<!-- render is embedded ruby that refers to the partial _header.html.erb, which is the bootstrap header -->
	<div class="container">
		<% flash.each do  |name, msg| %>
			<%= content_tag(:div, msg, lass: "alert alert-#{name}") %>
		<% end %>
		<%= yield %>
	</div>	

</body>
</html>
